package Projects.LibraryManagementSystem;

//Add books, view all books, borrow book, return book, search by title/author.

public enum ACTIONS {
    ADD,
    VIEW,
    SEARCH,
    BORROW,
    RETURN,
    EXIT
}